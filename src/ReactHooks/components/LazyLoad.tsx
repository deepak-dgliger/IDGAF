import React from 'react';

function LazyLoad() {

// 🔹 Why Use Lazy Loading?
// When your app has many components or images, loading everything upfront can slow down performance. Lazy loading fixes this by:
// Reducing initial bundle size
// Loading only what’s visible
// Improving user experience and speed

  return (
    <div>LazyLoad</div>
  )
}

export default LazyLoad